{
  "installCommand": "npm ci",
  "buildCommand": "npm run build -w @parallax/shared && npm run build -w @parallax/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
